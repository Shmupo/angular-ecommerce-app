export class User{
    constructor(
        // optional variables (?)
        public firstName?: string,
        public email?: string,
        public mobile?: string,
        public password?: string,
    ){}
}